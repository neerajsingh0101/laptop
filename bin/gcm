#!/bin/sh
#
# Easy way to add a commit message.
#
# Usage: gcm <commit message>
#
# Example: gcm added prettier

set -e

msg="$*"
[ -n "$msg" ] || { echo "Usage: $(basename "$0") commit message..."; exit 1; }

git commit -m "$msg"
