#!/bin/sh
#
# tree but only print directories
# by default the depth level is 1 but an argument can be passed
#
# Usage: treed [depth]
#
# Example: treed 3 - to have three levels of depth

tree -AdL "${1:-1}"
