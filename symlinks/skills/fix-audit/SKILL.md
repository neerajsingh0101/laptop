* Switch to main branch if you are not in worktree
* If you are in a worktree then go to neetozone folder and then go to the repo and there you can be in main branch and then do git pull
* git pull
* bundle install
* bundle exec neeto-audit -a
* If the audit fails then show error and stop
* If the audit passes then switch back to the feature branch
* git rebase
* bundle install
* bundle exec neeto-audit -a